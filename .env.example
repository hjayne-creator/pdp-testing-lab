# Optional: copy to .env at the repository root, or use backend/.env (recommended).
# The backend loads both; backend/.env overrides values from the root file.
#
# cp .env.example backend/.env
# Then fill in API keys in backend/.env and run the API from the backend directory.

SERPAPI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
FIRECRAWL_API_KEY=
